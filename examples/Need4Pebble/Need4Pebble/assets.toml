# Asset manifest for need_4_pebble.
#
# This is the single source of truth for content. No C file should name a PNG, a pixel
# offset, a sheet layout or a magic tile id -- everything reaches game code through the
# header the pipeline generates from this file.
#
# TOML rather than JSON so the reasoning behind the content can live beside it.

[project]
name = "need_4_pebble"
resources = "resources"
header = "src/c/assets_gen.h"
# The appstore rejects bundles over 256KB of resources. The device's hard cap is 1MB, but
# shipping is the constraint that actually binds.
budget_bytes = 262144
# Landscape, cluster along the top edge under both index fingers: right top button (DOWN)
# is gas, left top button (UP) is brake. See DESIGN.md.
orientation = "buttons_top"
device_address = "192.168.0.48"
force_screen_lock = true
# Sprites are 98% of this project's resource budget (touring/police/hangon_bike), so
# this is where compression actually matters. Bitplane (PNX_COMPRESS_MODE =
# PNX_COMPRESS_BITPLANE in src/c/pnx/pnx_config.h, project-wide -- applies to every
# sprite and every atlas alike, not a per-asset-type choice) in place of plain LZSS --
# this session's real-content measurements found it costs more flash on this project's
# busier/dithered sprites than LZSS does, but decodes each frame/tile independently,
# which LZSS's single combined stream cannot: traffic_car/police_normal/police_crash and
# ground/horizon_band all decode on demand through pnx_sprite_cache.c/pnx_tile_cache.c
# instead of being decoded whole into the arena at boot, which is what let them fit
# basalt-class platforms' scene arena at all. pnx_sprite_load/pnx_atlas_load themselves
# stay the loaders either way -- there is no separate "lazy" entry point or resource.
compress = "bitplane"

# Add content with the editor's Import and Fonts tabs, or by hand:
#
#   [[atlas]]   a tileset carved from a sheet
#   [[sprite]]  animation frames
#   [[font]]    a rasterised typeface  (a licence is required)
#   [[map]]     a grid of legend characters
#   [scene.*]   what loads together -- the only load point the framework has
#
# A map draws from one atlas by default -- the first one declared. To use several:
#
#   [[map]]
#   atlases = ["village", "forest"]   # the order fixes the map's tile id space
#
# and give a legend character an `atlas` key to say which one its role resolves against.
# A scene must NOT list a map's atlases: the map owns them, sizes its own pool and streams
# them by WorldTile, so naming one in a scene loads a second resident copy.

# ---------------------------------------------------------------- ground

[[atlas]]
name = "ground"
sheet = "art/ground.png"
tile = 16
region = [0, 0, 2, 2]
max_tiles = 4
out = "ground.bin"
# PLACEHOLDER art (4 flat-colour tiles), same role as road_chunk.png -- validates the
# ground-tile batching in draw_road end-to-end, not final art. Tiles 0/1 are the two
# alternating band colours draw_road's old ground fill already used (COLOUR_GROUND_A/B);
# 2/3 are an "amber marker light" accent, cycled by the anim clip below so the real
# device build exercises pnx_anim_frame on a genuine atlas tile id, not just the host
# test fixture in tests/test_assets.py.
[atlas.anim]
light = { frames = [2, 3], fps = 2, loop = true }

# ---------------------------------------------------------------- horizon band

[[atlas]]
name = "horizon_band"
sheet = "art/horizon_band.png"
tile = 32
region = [0, 0, 2, 3]
max_tiles = 6
colorkey = [85, 170, 255]
out = "horizon_band.bin"
# PLACEHOLDER art: a stylised synthwave cityscape silhouette. Tiles 0-3 are distinct
# building skylines (variety when tiled side to side); 4/5 are a lit-window accent
# variant of tile 1's skyline, cycled the same way ground.bin's "light" tile is --
# same pnx_anim_frame call, same generated-handle convention, a second real exercise
# of the tile-animation pipeline rather than a one-off.
#
# Drawn hand-rolled (draw_horizon_band, render.c), the same way draw_ground is, NOT
# through pnx_tilemap_draw_layer/pnx_layers_draw: that generic path writes screen
# coordinates directly in the TARGET's native space with no rotation hook, and this
# project's whole landscape orientation depends on fb_rect's own logical->physical
# transform being applied by hand at every draw call (buttons_top). A generic map/
# layer here would render sideways. No [[map]] for this reason -- just the atlas.
[atlas.anim]
lit = { frames = [4, 5], fps = 1, loop = true }

# ---------------------------------------------------------------- road chunk

[[sprite]]
name = "road_chunk"
sheet = "art/road_chunk.png"
# PLACEHOLDER art (a flat shoulder/road/shoulder band, no texture) -- generated to
# validate pnx_blit_4bpp_scaled end-to-end, not final art. Authored wide (cross-track,
# 180px = (ROAD_HALF_MAX + ROAD_HALF_MAX/RUMBLE_FRAC)*2, the widest total_half_width*2
# draw_road ever asks for) x thin (depth, 1px) -- the pack-time buttons_top rotation
# (rotate_dims) transposes this to a packed frame.w=1/frame.h=180, so the common case
# (height==1, i.e. no elevation stretch on this row) hits pnx_blit_4bpp_scaled's
# dst_w==src_w fast path. Two frames, matching draw_road's existing `alt` banding.
frames = [
    [0, 0, 180, 1],
    [0, 1, 180, 1],
]
out = "road_chunk.bin"

# ---------------------------------------------------------------- touring car

[[sprite]]
name = "touring_normal"
sheet = "art/NES_Touring_Car_Sprite_Sheet.png"
# 6 distance tiers (closest, row 0 -> farthest, row 5) x 9 steering angles (full left
# -> full right), row-major: frame = tier * 9 + angle. This is the traffic/opponent car
# at every depth a pseudo-3D road scales it to; the player's own car does not need the
# depth axis and can just hold tier 0.
frames = [
    [35, 177, 44, 23], [79, 177, 40, 23], [119, 177, 40, 23], [161, 177, 38, 23], [200, 177, 40, 23], [241, 177, 38, 23], [280, 177, 40, 23], [320, 177, 42, 23], [361, 177, 44, 23],
    [38, 204, 37, 18], [83, 204, 32, 18], [124, 204, 32, 18], [165, 204, 31, 18], [204, 204, 32, 18], [244, 204, 31, 18], [284, 204, 32, 18], [325, 204, 32, 18], [365, 204, 37, 18],
    [36, 228, 38, 17], [85, 228, 30, 17], [125, 228, 30, 17], [166, 228, 28, 17], [206, 228, 28, 17], [246, 228, 28, 17], [285, 228, 30, 17], [325, 228, 30, 17], [366, 228, 34, 17],
    [44, 250, 28, 14], [88, 250, 24, 14], [128, 250, 25, 14], [168, 250, 24, 14], [208, 250, 24, 14], [248, 250, 24, 14], [287, 250, 25, 14], [328, 250, 24, 14], [368, 250, 28, 14],
    [50, 270, 20, 10], [93, 270, 16, 10], [132, 270, 17, 10], [172, 270, 17, 10], [211, 270, 18, 10], [251, 270, 17, 10], [291, 270, 17, 10], [331, 270, 16, 10], [372, 270, 20, 10],
    [54, 288, 12, 7], [95, 288, 12, 7], [136, 288, 10, 7], [176, 288, 10, 7], [215, 288, 10, 7], [255, 288, 10, 7], [294, 288, 10, 7], [334, 288, 12, 7], [374, 288, 12, 7],
]
out = "touring_normal.bin"

# ------------------------------------------------------------------- traffic car
#
# Used to be `touring_normal` reused via a palette `variants` entry (a recolour only --
# `variants` requires identical frame dimensions, so it could never also make traffic
# smaller). Recolour first: the traffic sheet is the base art with its one body-green
# pixel value (85, 199, 83) programmatically remapped to orange (255, 170, 0), everything
# else (outline, brown accents, transparency) untouched, reported directly ("recolor the
# traffic cars so they stand out on the road") -- see `art/traffic.png`'s own generation,
# no hand-drawn art involved. Then, per a later direct ask ("make the traffic cars 10%
# smaller"), that recoloured sheet was resized 90% (`art/traffic_small.png`, PIL LANCZOS,
# a one-off offline script, not a build-time step) and every frame rect scaled by the
# same 0.9 and re-rounded from it -- a genuinely separate, smaller sprite, not another
# `variants` entry, since geometry (not just colour) had to change this time. Costs a
# real second copy of every frame rather than one shared palette slot, but at ~81% of
# touring_normal's own pixel area (0.9 linear -> ~0.81 area) that's an acceptable trade
# for a real size change `variants` structurally cannot express. draw_traffic (render.c)
# passes no palette override here -- the sheet's own pixels are already orange, unlike
# touring_normal's own green original.
#
# A few frames' width got nudged +1px past the exact 0.9 scale (e.g. the first row's
# 39->40, not 39): the pipeline requires each frame's own w*h to be EVEN (4bpp packs two
# pixels/byte, no per-row padding -- pack_sprite's own check, pnx_assets.py), and scaling
# a formerly-even area by a non-integer 0.9 doesn't reliably land back on an even product.
# Caught by the pipeline's own build-time validation, not a visual check.
[[sprite]]
name = "traffic_car"
sheet = "art/traffic_small.png"
frames = [
    [32, 159, 40, 21], [71, 159, 36, 21], [107, 159, 36, 21], [145, 159, 34, 21], [180, 159, 36, 21], [217, 159, 34, 21], [252, 159, 36, 21], [288, 159, 38, 21], [325, 159, 40, 21],
    [34, 184, 34, 16], [75, 184, 29, 16], [112, 184, 28, 16], [148, 184, 28, 16], [184, 184, 28, 16], [220, 184, 28, 16], [256, 184, 28, 16], [292, 184, 29, 16], [328, 184, 34, 16],
    [32, 205, 36, 15], [76, 205, 28, 15], [112, 205, 28, 15], [149, 205, 26, 15], [185, 205, 26, 15], [221, 205, 26, 15], [256, 205, 28, 15], [292, 205, 28, 15], [329, 205, 32, 15],
    [40, 225, 26, 13], [79, 225, 22, 13], [115, 225, 24, 13], [151, 225, 22, 13], [187, 225, 22, 13], [223, 225, 22, 13], [258, 225, 24, 13], [295, 225, 22, 13], [331, 225, 26, 13],
    [45, 243, 18, 9], [84, 243, 14, 9], [119, 243, 16, 9], [155, 243, 16, 9], [190, 243, 16, 9], [226, 243, 16, 9], [262, 243, 16, 9], [298, 243, 14, 9], [335, 243, 18, 9],
    [49, 259, 10, 7], [86, 259, 10, 7], [122, 259, 10, 7], [158, 259, 10, 7], [194, 259, 8, 7], [230, 259, 8, 7], [265, 259, 10, 7], [301, 259, 10, 7], [337, 259, 10, 7],
]
out = "traffic_car.bin"

[[sprite]]
name = "touring_crash"
sheet = "art/NES_Touring_Car_Sprite_Sheet.png"
# 0 impact flash, 1 spin, 2 side-on tumble, 3 final wreck. Frame 0 was [24, 432, 197,
# 24] -- 197px wide against the other three frames' 33-52px, because the source sheet
# actually draws a much longer tumble sequence at this row than the 4 frames pulled
# from it, with no gap between adjacent poses ("the first and second frame touch, no
# transparency between" -- confirmed directly, and separately confirmed visually: the
# old rect captured roughly the first FIVE poses of that sequence overlapping in one
# image, not one frame, which is what actually rendered as "multiple frames of the
# crash all at once"). Narrowed to just the first pose, same scale as frames 1-3.
frames = [
    [24, 432, 42, 24], [228, 432, 52, 23], [278, 434, 48, 18], [369, 434, 34, 19],
]
out = "touring_crash.bin"

[sprite.anim]
crash = { frames = [0, 1, 2, 3], fps = 8, loop = false }


# ---------------------------------------------------------------- police car

[[sprite]]
name = "police_normal"
sheet = "art/NES_Touring_police_Car_Sprite_Sheet.png"
# 6 distance tiers (closest, row 0 -> farthest, row 5) x 9 steering angles (full left
# -> full right), row-major: frame = tier * 9 + angle. This is the traffic/opponent car
# at every depth a pseudo-3D road scales it to; the player's own car does not need the
# depth axis and can just hold tier 0.
frames = [
    [35, 177, 44, 23], [79, 177, 40, 23], [119, 177, 40, 23], [161, 177, 38, 23], [200, 177, 40, 23], [241, 177, 38, 23], [280, 177, 40, 23], [320, 177, 42, 23], [361, 177, 44, 23],
    [38, 204, 37, 18], [83, 204, 32, 18], [124, 204, 32, 18], [165, 204, 31, 18], [204, 204, 32, 18], [244, 204, 31, 18], [284, 204, 32, 18], [325, 204, 32, 18], [365, 204, 37, 18],
    [36, 228, 38, 17], [85, 228, 30, 17], [125, 228, 30, 17], [166, 228, 28, 17], [206, 228, 28, 17], [246, 228, 28, 17], [285, 228, 30, 17], [325, 228, 30, 17], [366, 228, 34, 17],
    [44, 250, 28, 14], [88, 250, 24, 14], [128, 250, 25, 14], [168, 250, 24, 14], [208, 250, 24, 14], [248, 250, 24, 14], [287, 250, 25, 14], [328, 250, 24, 14], [368, 250, 28, 14],
    [50, 270, 20, 10], [93, 270, 16, 10], [132, 270, 17, 10], [172, 270, 17, 10], [211, 270, 18, 10], [251, 270, 17, 10], [291, 270, 17, 10], [331, 270, 16, 10], [370, 270, 20, 10],
    [54, 288, 12, 7], [95, 288, 12, 7], [136, 288, 10, 7], [176, 288, 10, 7], [215, 288, 10, 7], [255, 288, 10, 7], [294, 288, 10, 7], [334, 288, 12, 7], [374, 288, 12, 7],
]
out = "police_normal.bin"

[[sprite]]
name = "police_crash"
sheet = "art/NES_Touring_police_Car_Sprite_Sheet.png"
# 0 impact flash, 1 spin, 2 side-on tumble, 3 final wreck. Frame 0 narrowed 197->42,
# same fix and same reason as touring_crash's own frame 0 (this sheet is laid out
# identically to that one at this row, just recoloured) -- see that entry's comment.
frames = [
    [24, 432, 42, 24], [228, 432, 52, 23], [278, 433, 48, 19], [369, 433, 33, 20],
]
out = "police_crash.bin"

[sprite.anim]
crash = { frames = [0, 1, 2, 3], fps = 8, loop = false }


# --------------------------------------------------------------------------- menu

# The pause/game-over menus and the HUD event banner ("CHECKPOINT +500") -- uppercase
# and short by choice, so `extra` can just be every uppercase letter plus space/colon/
# digits/plus rather than tracking exact wording here. Digits+"+" were added alongside
# checkpoint/score scoring (render.c's draw_hud_event, draw_game_over's score line) --
# without them those draws silently render nothing for any character outside this set
# (FONT_ABSENT, pnx_assets.py), which is exactly what happened before this line changed.
#
# Was LiberationSans (a plain UI face), then "Monster Racing" (a display face, needing a
# `tracking` bump to stop its letters visibly touching at 14px -- reported "almost
# unreadable"), then briefly "Racing Energy" instead (a readability worry that turned out
# to already be fixed by the tracking bump -- confirmed via an emulator screenshot taken
# right before the swap). Landed here: BOTH faces at once, one packed font --
# `overlay_source`/`overlay_charset` (pnx_assets.py's own font-building code) rasterise
# just the digits/`+` from Monster Racing (whose numerals "looked fine") while every
# other character still comes from Racing Energy (the letters direct feedback preferred)
# -- "Numbers = Monster Racing. Text = Racing Energy," and "just combine all the
# rendered glyphs into a single font" rather than a game splitting strings at draw time
# to switch fonts mid-string (render.c's own draw_hud_event/draw_game_over used to do
# exactly that split; both are back to a single plain draw call now that this font does
# the combining instead). Still named "menu" (not renamed to e.g. "menu_small") -- every
# caller already references `PNX_ASSET_FONT_MENU`/`g->menu_font`, and this is still,
# semantically, "the menu font," just sourced from two faces now instead of one.
[[font]]
name = "menu"
source = "art/fonts/RacingEnergy-Personal.otf"
size = 14
depth = 1              # crisp: at this size, antialiasing muddies more than it smooths
threshold = 128
charset = "auto"
extra = "ABCDEFGHIJKLMNOPQRSTUVWXYZ :+0123456789"
license = "Racing Energy -- DEMO/personal use only, not cleared for redistribution"
overlay_source = "art/fonts/MonsterRacing-Personal.otf"
overlay_charset = "0123456789+"
overlay_license = "Monster Racing -- personal use only, not cleared for redistribution"
out = "font_menu.bin"

# The big numeric HUD readouts -- score, race timer (render.c's draw_hud) -- a stylised
# display face instead of the plain menu font, since these need to read at a glance
# while actually driving, not just during a paused/game-over screen. Pure Monster Racing,
# no overlay needed: this font's own charset is 100% digits/colon already, so there is no
# "text" portion for a second face to cover. The speedometer's own value uses the smaller
# "menu" font instead (render.c's draw_speedometer) once the gauge redesign dropped its
# "N MPH" text for a bare number above the gauge's own top bar -- small enough there that
# the big face didn't suit it anymore, and menu's own digits (the overlay above) are the
# same Monster Racing numerals this font uses, just at 14px instead of 20px.
[[font]]
name = "hud"
source = "art/fonts/MonsterRacing-Personal.otf"
size = 20
depth = 1
threshold = 128
charset = "0123456789:"
# Free for personal/non-commercial use per the source distribution (no OSI licence
# accompanies it) -- fine for this project as currently built and run locally, but a
# blocker if Need4Pebble is ever submitted to the Pebble appstore or otherwise
# distributed; a commercial licence or a different display face would be needed first.
license = "Monster Racing -- personal use only, not cleared for redistribution"
out = "font_hud.bin"
